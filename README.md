# 💰 Zarplata - Система розрахунку зарплати

Веб-додаток для обліку робочого часу та розрахунку зарплати працівників.

## 🚀 Технології

- **Frontend**: React 18 + TypeScript + Vite
- **Backend**: Node.js + Express + TypeScript
- **База даних**: SQLite3 (better-sqlite3)
- **Інструменти розробки**: tsx, concurrently

## 📁 Структура проекту

```
Zarplata/
├── src/
│   ├── client/          # React фронтенд
│   │   ├── App.tsx      # Основний компонент
│   │   ├── App.css      # Стилі додатку
│   │   ├── main.tsx     # Точка входу React
│   │   ├── index.html   # HTML шаблон
│   │   └── index.css    # Глобальні стилі
│   └── server/          # Express бекенд
│       ├── index.ts     # Точка входу API сервера
│       ├── database.ts  # Модуль роботи з БД
│       └── routes.ts    # API маршрути
├── data/
│   ├── payroll.db       # База даних SQLite (створюється автоматично)
│   └── backups/         # Резервні копії БД
├── scripts/
│   └── init-db.js       # Скрипт ініціалізації БД
├── public/              # Статичні файли
├── dist/                # Зібрані файли (генерується)
├── package.json
├── tsconfig.json
├── tsconfig.server.json
└── vite.config.ts
```

## 📊 Структура бази даних

### Таблиця `employees` (Працівники)
- `id` - Унікальний ідентифікатор
- `full_name` - ПІБ працівника
- `position` - Посада
- `hourly_rate` - Погодинна ставка
- `hire_date` - Дата прийняття на роботу
- `status` - Статус (active/inactive)

### Таблиця `work_hours` (Робочі години)
- `id` - Унікальний ідентифікатор
- `employee_id` - ID працівника
- `work_date` - Дата роботи
- `hours_worked` - Відпрацьовані години
- `overtime_hours` - Понаднормові години
- `notes` - Примітки

### Таблиця `payroll` (Зарплата)
- `id` - Унікальний ідентифікатор
- `employee_id` - ID працівника
- `period_start` - Початок періоду
- `period_end` - Кінець періоду
- `regular_hours` - Звичайні години
- `overtime_hours` - Понаднормові години
- `gross_salary` - Зарплата до вирахувань
- `tax_amount` - Сума податків
- `net_salary` - Зарплата після вирахувань
- `status` - Статус (pending/paid/cancelled)

## 🛠️ Встановлення

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd Zarplata
```

2. Встановіть залежності:
```bash
npm install
```

3. Ініціалізуйте базу даних:
```bash
npm run db:init
```

## 🏃 Запуск проекту

### Режим розробки
Запустити одночасно фронтенд (порт 3000) та бекенд (порт 3001):
```bash
npm run dev
```

Або окремо:
```bash
# Тільки фронтенд
npm run dev:client

# Тільки бекенд
npm run dev:server
```

### Збірка для продакшн
```bash
npm run build
```

### Запуск продакшн версії
```bash
npm start
```

## 🔌 API Endpoints

### Працівники
- `GET /api/employees` - Отримати список працівників
- `GET /api/employees/:id` - Отримати працівника за ID
- `POST /api/employees` - Додати нового працівника
- `PUT /api/employees/:id` - Оновити дані працівника
- `DELETE /api/employees/:id` - Деактивувати працівника

### Робочі години
- `GET /api/work-hours/:employeeId?start=YYYY-MM-DD&end=YYYY-MM-DD` - Отримати години працівника
- `POST /api/work-hours` - Додати робочі години
- `PUT /api/work-hours/:id` - Оновити робочі години

### Зарплата
- `GET /api/payroll?start=YYYY-MM-DD&end=YYYY-MM-DD` - Отримати записи про зарплату
- `GET /api/payroll/:employeeId` - Отримати зарплату конкретного працівника
- `POST /api/payroll` - Створити запис про зарплату
- `PATCH /api/payroll/:id/status` - Оновити статус зарплати

## 📝 Приклад використання API

### Додавання нового працівника
```bash
curl -X POST http://localhost:3001/api/employees \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Іванов Іван Іванович",
    "position": "Розробник",
    "hourly_rate": 250,
    "hire_date": "2024-01-15",
    "status": "active"
  }'
```

### Додавання робочих годин
```bash
curl -X POST http://localhost:3001/api/work-hours \
  -H "Content-Type: application/json" \
  -d '{
    "employee_id": 1,
    "work_date": "2024-01-20",
    "hours_worked": 8,
    "overtime_hours": 2,
    "notes": "Робота над проектом"
  }'
```

## 🧪 Тестування

```bash
# Буде додано пізніше
npm test
```

## 📦 Залежності

### Production
- `better-sqlite3` - SQLite драйвер
- `express` - Веб-фреймворк
- `cors` - CORS middleware
- `react` - UI бібліотека
- `react-dom` - React DOM рендерер

### Development
- `typescript` - TypeScript компілятор
- `vite` - Інструмент збірки фронтенду
- `tsx` - TypeScript виконавець для Node.js
- `concurrently` - Запуск декількох команд одночасно

## 📄 Ліцензія

ISC

## 👤 Автор

[Ваше ім'я]

## 🤝 Внесок

Запрошуємо до співпраці! Будь ласка, створюйте issues та pull requests.
